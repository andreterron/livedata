// import { graph, edge } from '../../src/v2/graph';
import { AppState, OpenFile, OpenSnippet, Position } from './test-types';
import { ILiveModel } from '../../src/v2/base';

let stateModel: ILiveModel<AppState>;

// stateModel.pipe(edge("openFiles"));

// let state = graph(stateModel); //.openFiles.$.subscribe()
